services:
  - type: web
    name: genesis-engine
    env: static
    buildCommand: echo "Static site"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
